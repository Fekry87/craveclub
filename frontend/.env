# Vite loads this file in EVERY mode, production included. Anything put here leaks
# into the production bundle, so keep it empty of environment-specific values.
# Dev-only values live in .env.development; production values come from
# .env.production plus the Railway frontend service's own variables.
