[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --prefer-offline"]
cacheDirectories = []

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -p ${PORT:-3000}"
