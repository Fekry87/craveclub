# Loaded by Vite ONLY in dev (npm run dev). Never reaches a production build.
# These must match backend/.env for local real-time to work.
VITE_REVERB_APP_KEY=fw733ttnvuuuiqcii0rv
VITE_REVERB_HOST=localhost
VITE_REVERB_PORT=8080
VITE_REVERB_SCHEME=http
